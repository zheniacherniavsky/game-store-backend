version: '3.8'
services:
  app:
    build: .
    ports:
      - 3000:3000
    env_file: .env
    depends_on:
      - db
  db:
    image: 'mongo'
    environment:
      MONGO_INITDB_DATABASE: itechart
    ports:
      - 27017:27017
